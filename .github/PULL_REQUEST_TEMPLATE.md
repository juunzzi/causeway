## 배경

<!-- 무엇이 문제였는지 / 왜 필요한지. 이슈가 있으면 아래 한 줄로 연결한다. -->

Closes #

## 작업내용

<!-- 무엇을 바꿨는지. 설계 판단이 갈렸다면 왜 이쪽을 골랐는지도. -->

-

## 확인

- [ ] `pnpm check && pnpm typecheck && pnpm test` 통과
- [ ] 동작이 바뀌었다면 문서(`README` / `SETUP` / `CLAUDE` / `docs/`)도 같은 PR 에서 갱신
- [ ] 세션 권한을 넓히는 변경(`SESSION_ALLOWED_TOOLS`·`settingSources`·가드 훅)이면 무엇이 열리는지 위에 명시
