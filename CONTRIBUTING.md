# 기여 가이드

이슈로 먼저 이야기하고, 브랜치를 파고, PR 로 머지한다. `main` 에는 직접 push 하지 않는다.

## 흐름

1. **이슈를 연다** — 버그면 재현 절차, 제안이면 "무엇이 안 되는가"부터. 한 줄짜리 오타 수정은
   이슈 없이 바로 PR 이어도 된다.
2. **브랜치를 판다** — `feat/…` · `fix/…` · `docs/…` · `chore/…` (kebab-case).
3. **커밋한다** — `type: 한 줄 설명` (`feat:` `fix:` `docs:` `refactor:` `test:` `chore:`).
4. **PR 을 연다** — 템플릿을 채우고, 이슈가 있으면 본문에 `Closes #N`.
5. **CI 가 초록불이면 squash merge** 하고 브랜치를 지운다.

## 로컬에서 먼저 통과시킬 것

```bash
pnpm check && pnpm typecheck && pnpm test
```

lefthook 이 pre-commit 에서도 같은 걸 돌리므로, 커밋이 느리다고 `--no-verify` 로 우회하면
PR 에서 같은 자리에 다시 걸린다.

Node 24+ 가 필요하다 — `node:sqlite` 내장을 쓴다.

## CI 가 보는 것

| 검사 | 언제 | 막나 |
|---|---|---|
| `check` (`biome` · `tsc` · `vitest`) | PR · main | ✅ 머지 차단 |
| `gitleaks` | PR · main | ✅ 머지 차단 |
| **PR 제목 규약** | PR (제목 수정 시 재검사) | 위 표의 `type: 설명` 형식 |
| **의존성 심사** | PR | 새로 끌어오는 의존성의 알려진 취약점(high 이상)·라이선스 |
| **CodeQL** | PR · main · 주 1회 | ❌ — 결과는 Security 탭으로 간다 |

PR 제목을 따로 검사하는 것은 **squash merge 라 그 제목이 곧 `main` 의 커밋 메시지**가 되기
때문이다. CodeQL 을 머지 게이트로 삼지 않는 것은, 정적 분석의 오탐이 상시 빨간불이 되면
그때부터 아무도 결과를 안 보기 때문이다.

의존성 갱신 PR 은 Dependabot 이 주 1회 묶어서 연다(major 는 개별 PR). 자동 머지되는 것은
없고 위 게이트를 그대로 통과해야 한다.

## 코드에서 지키는 것

- **순수부와 부작용부를 파일 안에서 가른다.** 가드·파서·요약은 순수 함수여야 테스트가 계약을
  붙잡는다. 부작용은 주입받은 의존성으로만 낸다.
- **경계는 프롬프트가 아니라 스키마로 만든다.** 조회 전용 도구에 `method` 필드를 두지 않는 것이
  "쓰기 하지 마"라고 적어 두는 것보다 강하다.
- **새 도구·새 잡은 등록 배열에 손으로 한 줄 추가한다.** glob 자동발견을 쓰지 않는 이유는
  그 배열이 곧 노출 경계이고, 리뷰가 그 한 줄에서 끝나야 하기 때문이다.
- **실패는 시끄러워야 한다.** 배선이 빠졌으면 부팅 로그에 한 줄 남긴다 — 조용한 실패는
  "아무 일도 없었던 것"과 구분이 안 된다.
- 왜 그렇게 했는지가 자명하지 않은 코드에는 **이유**를 주석으로 남긴다. 무엇을 하는지는 코드가
  이미 말하고 있다.

## 문서

동작이 바뀌면 문서도 **같은 PR 에서** 고친다. 설계 근거는 [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md),
설치·자격증명은 [SETUP.md](SETUP.md), 운영은 [CLAUDE.md](CLAUDE.md) 가 각각 SoT 다 — 같은 내용을
여러 곳에 복사하지 않는다.

## 보안

- 토큰·키·비밀번호를 커밋하지 않는다. 새 설정값은 `.env.example` 에 **빈 템플릿과 설명**으로만 남긴다.
- 테스트에 "진짜처럼 생긴" 가짜 시크릿이 꼭 필요하면 `.gitleaks.toml` 의 allowlist 에 파일 단위로
  좁게 추가한다. CI 의 gitleaks 스캔이 그 밖은 전부 막는다.
- 이 봇의 세션은 호스트에서 Bash 를 돌린다. 권한을 넓히는 변경(`SESSION_ALLOWED_TOOLS`,
  `settingSources`, 가드 훅)은 PR 본문에 **무엇이 열리는지** 명시한다.
